Updates Made:
    1.Button animations added.
    2.Payment success page added.
    3.All pages are linked using buttons.

Note
    1.Epay Tax page2 has around 2000 lines of code because of pagination.
    2.Tags like a, form has global stying so if adding them update their style using css for that page only.
    3.For button events to work need to use Javascript. 

Guide
    1.To show error message change the class of error message div to "error".
    2.To hide error message change the class of error message div ot "noError".
    3.Javascript used in pages can be found in the js folder with same name as page , if its not there then the html page has script tag at the bottom.

